# Experiment running time, experiment name and scenario
experiment_time = 30
experiment_name = "experiment1"
scenario_file = 'experiment.scenario'

# Single process on localhost
experiment_server_cmd = 'experiment_server.py'
sync_host = 127.0.0.1
sync_port = __unique_port__
sync_experiment_start_delay = 1
sync_subscribers_amount = 1
local_instance_cmd = "process_guard.py -c launch_scenario.py -t $EXPERIMENT_TIME -m $OUTPUT_DIR -o OUTPUT_DIR"
post_process_cmd = 'post_process_ipv8_experiment.sh'
use_local_venv = TRUE